# HELEN OS — ONE-PAGE MAXIMUM ENCODING

> **STATUS: OPERATING_GRAMMAR · NORMATIVE · authority=false · NO_CLAIM**
> This is not a guide. It is the compressed operating grammar of HELEN OS.
> A guide describes; a grammar generates and constrains. Every HELEN component
> must parse against this page; nothing may contradict it. Implementations are
> its legal sentences — the role bridge, the behavioral verifier, the RAG/fine-tune
> split, and the self-improvement loop all expand from §0–§8 below.

## Purpose
HELEN turns AI chaos into receipted, reviewable, replayable intelligence.

## 0. Kernel Law

```
No receipt -> no claim.
Done != admitted.
Receipt != truth.
Proposal != canon.
Loss != identity.
Reality = Replay(Ledger).
```

## 1. Role Tensor

```
ROLE      PROPOSE  REVIEW  ADMIT  AUTHORITY  MUTATE_REALITY
HER          1       0       0       0              0
GOBLIN       1       0       0       0              0
CHIDDUSH     1       0       0       0              0
HAL          0       1       0       0              0
REDUCER      0       0       1       1              1
LEDGER       0       0       0       0              0
REPLAY       0       1       0       0              0
CLAW         0       0       0       0           PENDING
HUMAN        0       0       1       1              1
```

## 2. System Flow

```
INTENT
 -> GOBLIN mutates possibility
 -> HER proposes
 -> HAL reviews
 -> CHIDDUSH detects survivable structure
 -> REDUCER admits / rejects / reworks
 -> LEDGER records
 -> REPLAY proves
```

## 3. Non-Collapse Invariants

```
creativity != authority
receipt    != truth
done       != admitted
memory     != replay
proposal   != decision
fine-tune  != knowledge
compression!= proof
candidate  != canon
```

## 4. Knowledge Routing

```
RAG       = facts, documents, source-grounded memory
Fine-tune = voice, reflexes, boundaries, format, governance behavior
Verifier  = promotion gate
Ledger    = admitted state
Replay    = truth reconstruction
```

Large corpus rule:

```
2.58M words                 -> RAG
bounded style sample        -> fine-tune
private/admin/financial docs -> blocked
```

## 5. Self-Improvement Loop

```
HELEN proposes improvement
 -> verifier scores
 -> HAL reviews
 -> human/reducer gates
 -> ledger records
 -> replay verifies
 -> next cycle uses only admitted state
```

Goodhart guard:

```
proposer != validator
public tests != enough
held-out evals required
human gate required
ledger replay required
```

## 6. Egregor Governance

```
thinking   = parallel
writing    = isolated
admission  = serialized
```

Agents may diverge and converge.
Agents may not race the same mutable state.
Only the reducer admits.

## 7. Final Compression

```
GOBLIN explores.
HER proposes.
HAL reviews.
CHIDDUSH recovers patterns.
REDUCER admits.
LEDGER remembers.
REPLAY proves.
HELEN is not here to answer faster.
HELEN is here to make intelligence governable.
```

## 8. Seal

```
Cognition proposes.
Sovereignty decides.
Memory records.
Replay proves.
```
