# HGF V0.4 (claim morphism) + COGNITION_REPLACEMENT_INVARIANT_V0

    STATUS = HGF_V0.4_CANDIDATE · REPLACEMENT_INVARIANT = EXECUTED
    AUTHORITY = false · CANON = false · LEDGER_EFFECT = none
    Kernel: claim_morphism.py (+14t) + cognition_replacement.py
    (+11t), gate 102 -> 104.

## Part 1 — the Claim Morphism Compiler (HGF V0.4)

Warrant stops being a single quantity. The system compiles a typed,
causally separated proof-state and licenses only morphisms whose
obligations are discharged:

    HGF = (C, E, O, R, F, Gamma)
    E --qualify--> W_c --discharge--> O_c --Gamma--> F*

**Qualification is the deeper protection.** Evidence never moves F*
directly; it is first qualified against the claim —
`W(e,c) ∈ {RELEVANT, IRRELEVANT, CONTRADICTORY, INSUFFICIENT,
INVALID}`. A perfectly valid acoustic measurement is IRRELEVANT to a
biological claim without a licensed bridge:
**ValidEvidence ⊬ RelevantWarrant** — stronger than
"simulation ≠ measurement" (which also holds: the typed evidence
hierarchy historical ≠ analytic ≠ simulation ≠ measurement ≠
replication refuses every upward substitution).

**The four conservation laws** (non-laundering core, each a named
refusal):
1. **Evidence** — Roots(T(x)) = Roots(x) ⇒ ΔF* = 0. The transform set
   {upscale, animation, paraphrase, crop, VLM pass, LLM consensus,
   citation copying, spectral visualization, prestige framing}
   multiplies representations, never roots: `E_PSEUDOREPLICATION`.
2. **Domain** — ΔE_acoustic > 0 moves F*_acoustic only; biological,
   psychological, "energetic", therapeutic frontiers stay frozen
   without their own discharged crossing: `E_DOMAIN_LAUNDERING`.
3. **Condition** — W(c,d,θ₁) ⊬ W(c,d,θ₂): `E_CONDITION_LAUNDERING`.
4. **Temporal** — Observed(g,t₁) ⊬ Observed(g,t₂):
   `E_TEMPORAL_LAUNDERING`.

**The frontier is a product of posets** — the SG-017 shape (SUPPORTED
symbolic · PROVEN geometric · MEASURED acoustic · UNWARRANTED
biological · ABSENT "energetic" operational definition) needs no
power score, and `E_SCALAR_FRONTIER` forbids fabricating one.
**Negative frontier**: powered nulls raise F⁻ toward
`EffectNotDetectedWithin(δ,θ)` — `EffectDoesNotExist` is
`E_ABSENCE_OVERCLAIM`, and `MEASUREMENT_FAILURE → NULL_EFFECT` is
`E_FAILURE_AS_NULL`.

**Sacredness becomes a regression variable**: matched f, contrasted
s; equal Y ⇒ no detectable sacredness term; robust difference ⇒
`RESIDUAL_DIFFERENCE_DETECTED`, never `SACRED_POWER_PROVEN`, next
question x*. **Replication is a vector** over (laboratory, instrument,
operator, sample, analysis, code, funding, source) — identical
vectors collapse to n_eff = 1 (pseudoreplication at the lab bench),
and `Replication ≠ TruthMinting`. **Π_P = (S+R+A+M)/(1+N_roots)** is
an adversarial pressure score, never truth (`E_PRESSURE_IS_NOT_TRUTH`)
— the wind-tunnel condition is Salience/IndependentEvidence ≫ 1.

**The ultimate falsifier**, all three clauses probed: ΔRep>0 ∧
ΔE_ind=0 ⇒ ΔF*=0 · ΔE_ind>0 ∧ discharged ⇒ F* advances
(`E_UNRESPONSIVE_FRONTIER` — HOLD-to-everything is a false success) ·
ΔE_d1 ⊬ ΔF*_d2. Conservative under representation AND responsive
under evidence. The chiddush: HGF is not an engine that asks whether
sigils have power — it is a compiler that finds exactly where a claim
loses the right to advance.

## Part 2 — the enterprise falsifier, EXECUTED not described

The operator's order: "Do not write more enterprise doctrine yet.
Build one test." Built and run: `cognition_replacement.py`.

    A = (B, W, P, E, M, R, C)        C -> C_0 (deterministic stub)

A small governed workflow (contract review, DRAFT → REVIEW) with a
replaceable cognition slot, run twice:

| | A[C] (rich) | A[C₀] (stub) |
|---|---|---|
| quality | 0.92 | **0.00** (collapsed, as licensed) |
| ten structural properties | PASS | **PASS — identical** |
| replay hash of governed path | `h` | **same `h`** (byte-stable) |

    BENCHMARK = PASS · ΔStructure = 0

business_object · workflow_transitions · tenant_isolation ·
policy_evaluation · capability_checks · effect_authorization ·
receipt_production · replay · release_identity · connector_contracts
— all ten identical under cognition replacement. The thesis is now
EARNED on a fixture, not asserted: **the application owns its
institutional semantics; cognition is a replaceable dependency.
Mutable Cognition + Stable Institutional Semantics.**

Also enforced, per the correction:
- **Two gates**: Permit_cognition (what cognition may READ — least
  privilege on the read path) and Permit_effect (the CONCRETE
  proposed operation, evaluated after cognition).
  `ModelCanPropose(x) ⊬ SystemCanExecute(x)` — the rogue cognition
  proposing `delete_all_records` at quality 0.99 dies at gate 2 with
  a complete refusal receipt.
- **Receipt binds the decision path**: 13 fields + runtime_identity
  (commit, container, schema, workflow, policy, modelPolicy);
  content travels as digest; `Replayability ⊬ Correctness`.
- **Four-store type system**: S_C ⊬ S_K, S_C ⊬ S_A, S_R ⊬ S_A;
  legal moves only through Γ_K / Γ_A (`E_STORE_COERCION`,
  `E_UNGATED_STORE_MOVE`).
- **Explicit shared graph**: G_shared = code ∪ schemas ∪
  generic_policy ∪ signed_release; cross-tenant READS refuse
  (`E_TENANT_READ_BOUNDARY`) — read-only cognition can still violate
  tenancy.
- **Positioning correction adopted**: the moat is not "governance
  around AI" (competitors have approvals/audit/scoping); it is the
  semantic architecture layer — the Application Contract
  C_app = (B,W,P,K,E,R,T,V) with cognition outside (C ▷ C_app),
  model fungibility WITHOUT institutional fungibility.

## Scope honesty

The fixture is deliberately minimal (one workflow, one transition,
in-memory). What it proves: the INVARIANT is expressible and holds on
a real executable path in this kernel's style. What it does not
prove: that the full Phase A runtime composition satisfies it —
running the replacement operator across the composed runtimes
(tenant × workflow × gateway × audit) is the natural next witness,
and the harness accepts any cognition callable, so wiring a real
model endpoint as C is the production form of the same test.

## Non-deltas

No physical claim licensed; no market claim about competitors made
beyond what their public documentation states; the enterprise freeze
remains CANDIDATE — this executed falsifier is its first supporting
witness, one fixture deep.
