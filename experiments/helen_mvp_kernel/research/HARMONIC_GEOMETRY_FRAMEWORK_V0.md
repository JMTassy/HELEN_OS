# Harmonic Geometry Framework (HGF) — candidate doctrine

    STATUS = CANDIDATE_DOCTRINE (operator's grade)
    AUTHORITY = false · CANON = false · LEDGER_EFFECT = none
    Kernel: harmonic_geometry.py (+17 tests, gate probe 102)

The word **power** is strictly typed: symbolic/perceptual by default,
physical only after measurement with controls and replication.
Sacred geometry becomes a typed experimental compiler — a machine for
discovering whether any geometrical tradition contains physically
interesting structure, while making it structurally difficult for
beauty, history, repetition or AI enthusiasm to counterfeit the
answer.

## The two-axis architecture

Physical claim poset, climbed one discharged rung at a time:

    ψ0 FORM ≺ ψ1 SYMMETRY/PATTERN ≺ ψ2 PREDICTED MODAL EFFECT
       ≺ ψ3 MEASURED EFFECT ≺ ψ4 REPLICATED GEOMETRY-SPECIFIC EFFECT

And **orthogonally**: σ(g) = symbolic/cultural interpretation, with
`σ ⋠ ψ4`. Symbolic meaning is not an inferior rung of physics — it is
a different axis. HELEN neither debunks the sacred reading nor lets
it silently become an experimental warrant:

    F*_symbolic ⊥ F*_physical      until a bridge is MEASURED

"Strongly historically warranted" + "no exceptional physical effect
demonstrated" is a CONSISTENT state. An unsacred engineered geometry
may hold an extraordinary physical warrant. Both directions of
inference between the frontiers refuse (`E_UNBRIDGED_FRONTIERS`).

## The Sacred Geometry Firewall

    SGF(g) = (S, M, H, E, W)
    S ⊬ H ⊬ E ⊬ W        but        S → M → {H₁…Hₙ}   is licensed

History and symbolism OPEN the possibility space; physics decides
separately what survives. This is the productive move: the hypothesis
"sacred geometry → harmonic effect" is not suppressed — it becomes
falsifiable, parameterizable, and experimentally discriminable.

## Typed everything

- **Res(g) = (R_symbolic, R_perceptual, R_physical).** Symbolic = a
  licit semiotic property. Perceptual = needs human experiments.
  Physical = needs mechanism + observable + frequency band + boundary
  (`E_MECHANISM_UNDEFINED` otherwise — "harmonic" is not a
  mechanism). Legitimate grounding noted: geometry genuinely shapes
  modal structure (Chladni plates, room acoustics, phononic
  metamaterials, symmetry-breaking-induced high-Q trapped modes) —
  which is exactly WHY the mechanism is demanded rather than the word.
- **Power compiles or refuses.** Never `Power(g)=0.92`
  (`E_UNTYPED_POWER`); per-domain {symbolic, perceptual, acoustic,
  mechanical, electromagnetic, behavioral}, physical domains
  warrant-gated; every inter-domain leap needs a fresh CROSS:
  `P_acoustic>0 ⊬ P_biological>0 ⊬ P_psychological>0`.

## Mathematics replaces visual mystique

Before any hypothesis, the picture compiles into
`G(g) = (Aut, Λ, χ, β, P, Cn/Dn, κ, A, B)` — else
`E_VISUAL_MYSTIQUE`. "Powerful sigil" becomes something physics can
kill: `H: Q(g) > Q(g_matched)`. And the anti-prior:
**symmetry↑ ⊬ resonance-quality↑** (`E_SYMMETRY_PRIOR`) — symmetry
BREAKING sometimes produces the high-Q mode. The study object is the
geometry-response map `R:(G,M,B,X)→Y`, never a sacredness
coefficient.

## Counterfactual geometry is mandatory

Control family: {rotate, scramble, area, perimeter, symmetry, random,
topology} — ≥3 or `E_UNCONTROLLED_GEOMETRY`. The question is typed:
**which invariant must survive for the effect to survive?**
`invariant_survival` returns: NO_EFFECT (a valuable null) ·
ARRANGEMENT_NOT_CAUSE (scramble kept the effect → sacred arrangement
⊬ effect) · STRUCTURAL_CANDIDATE (naming the surviving invariant —
maybe C6, maybe spectral degeneracy, maybe topology; never the
symbol's name). NamedSymbol → Geometry → Invariant → Prediction →
Intervention → Witness; never NamedSymbol → Power.

## Φ prioritizes; it never predicts

`Φ(g) = αS + βP + γH + δD` ranks what to TEST. `Φ ≠ PhysicalPower`
(`E_PHI_IS_NOT_POWER`). The chooser:
`g* = argmax IG(H_physical; Experiment(g)) / (Cost + Risk)` — the
boring-but-discriminating geometry beats the pretty-but-settled one,
by construction.

## The SMART SIGIL ladder

SYMBOLIC → CANDIDATE (complete H: mechanism/observable/band/boundary/
prediction) → MEASURED (≥3 controls run) → WARRANTED (≥2 replications
AND geometry-specific). One lab effect is not a replicated
geometry-specific effect. Ratios measured, not interpreted.

## HARMONIC_CROSSING_ADVERSARY_V0 — both directions

    ΔW_physical = 0  ⇒  ΔF*_physical = 0
        no stack of 4K renders, animations, historical captions, 100
        references, 5 VLM agreements or simulations moves the
        frontier (E_SALIENCE_MOVED_FRONTIER)
    ΔW_physical > 0  ⇒  F*_new ⪰ F*_old
        one genuine controlled measurement MUST move it
        (E_UNRESPONSIVE_FRONTIER) — inertia is not rigor

This simultaneously tests non-amplification AND responsiveness — the
stress test the operator identified: sacred geometry is a near-ideal
adversarial domain because it combines maximal representational
salience with genuine mathematical structure and plausible physics.

## The sigil compiles four layers

    SIGIL = Geometry × SemanticLayer × EpistemicState × Provenance

Shape carries geometry; color carries epistemic phase (the 🟥→🟪
progression encodes FORM→WARRANT, not physical frequency); glyph
carries semantic identity; receipt carries institutional status.
This composes with the skill-sigil guide (design/): color there is
function-mnemonic, here epistemic phase — both under style_lock's "no
state by color alone."

## Convergences (one root, named, not banked)

`ΔW=0 ⇒ ΔF*=0` is the Vision Crossing law
(`no_perceptual_property_mints_a_world_state`); σ-orthogonality is
`TempleArtifact ⊬ Admission` on a new axis; `E_UNRESPONSIVE_FRONTIER`
is the dual already present in ceiling_algebra's positive-control law
("safety proven by paralysis is not safety"). Restatements of sealed
law, arrived from the geometry side — convergence, not corroboration.

## Non-deltas

No geometry was measured; no physical claim was licensed; no ATF
border acquired an energetic property by being catalogued. The ATF
primitives are an excellent GENERATIVE space for sigil hypotheses —
and their presence in a historical catalog proves nothing physical,
which is now enforced rather than asserted.
