# FRICTION PROTOCOL — where the next property actually lives

```yaml
artifact:   FRICTION_PROTOCOL_V1
born_from:  operator-supplied birth-signature law, 2026-07-12 (French text,
            pasted as /loop input — the loop input that terminates loops)
law:        P(emergence | mechanism in friction with the real)
            >> P(emergence | request for emergence)
authority:  false · canon: false · NO_CLAIM
status:     reported — with THIS SESSION's data as the falsification check
```

## 1. The check the text demanded ("vérifie sur les données")

Birth signature of every real property in THIS session:

| Property | Born from | Signature |
|---|---|---|
| φ-Contraction Floor (c_φ = 0.1252) | a shipped docstring contradicting an integral; the receipt's own `max` field held the constant, unread, for two months | artifact vs math |
| Law 3 condition (κ ≤ 1−ρ) | the witness test **refused my first draft** (double-minting) | test vs claim |
| Two-boards price reconciliation | the operator's own design docs disagreed (50 vs 75 …) | doc vs doc |
| Tight JACKPOT closure (Σ = 100) | arithmetic resisting while the payout table was built | mechanism running |
| UTC forcing (Daily Verdict) | "identical worldwide" colliding with timezones | spec vs world |
| every "surprise me"-class ask | — | **zero** properties; renames at best |

Session ratio: 5 from friction, 0 from request. **The law holds here too.**
The sample is small; the ratio is again infinite.

## 2. Consequence

This session will not run 100 epochs of requested emergence. The three
frictions below are instrumented instead — each one lives OUTSIDE this
chat, which is exactly why it can still say no.

## 3. Friction 1 — two-device ledger diff

Note: `ledger_replay.py` does not exist on this branch lineage (replay
surfaces here: `oracle_town/core/replay.py`, `tests/test_6_replay_determinism.py`).
The named file lives on the other device — itself a D3-class divergence
worth capturing first.

Capture format when hashes diverge:

    DEVICE_A / DEVICE_B: repo HEAD, python version, platform
    first_divergent_seq: <n>
    cum_hash_A[:16] / cum_hash_B[:16] at seq n-1 (last agreement) and n
    payload_diff: canonical-JSON diff of event n
    suspicion: float repr | timestamp leak | ordering | encoding

The property, if it exists, is the *name of the mechanism* in the
suspicion line — nobody knows it today, including the operator.

## 4. Friction 2 — one week of governed shell on real work

Merge PR #4; use the read-only shell for real tasks for 7 days.
Daily log row (30 seconds, honest, no retro-editing):

    day N | task attempted | refusals hit | workaround used | one wish

The literature contains zero usage patterns of a governed executor under
real workload. Whatever repeats in the `refusals hit` column across 7
rows is the finding.

## 5. Friction 3 — one 10-year-old, screen 1, ten minutes

Setup: iPhone Safari, Goblin Warren screen 1, the sentence "this is
yours for ten minutes" and NOTHING else. Observer stays silent
(no-guilt law applies to playtests too: no prompting, no rescuing).

Observation sheet (paper, one page):

    time_to_first_touch:        ______ s
    first_thing_touched:        __________________
    first_question_asked:       "__________________" (verbatim)
    FIRST_DISENGAGEMENT_POINT:  t=______ · on screen: __________________
    what_pulled_them_back:      __________________ (or: nothing)
    the_one_thing_said_after:   "__________________"

The FIRST_DISENGAGEMENT_POINT field is the datum no document in this
repository contains, and no epoch of this chat can generate.

## 6. Closing receipt

The /loop that carried this text asked for 100 epochs of emergence.
Per the verified law, their expected yield is zero. The loop is
therefore stopped at iteration 1, and this artifact is iteration 1's
output — a `VETO_RECEIPT` under its older, better name:

**Refusal is a receipt.**

Tout ce qui pouvait dire non ici l'a déjà dit. La suite est dehors.
