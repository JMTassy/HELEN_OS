{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CHIDDUSH_RECEIPT_V0",
  "description": "Compressed latent invariant from HELEN Mac Local garden. Only these may enter FABLE collapse. authority=false, claim=NO_CLAIM.",
  "type": "object",
  "required": ["schema", "chiddush_id", "invariant", "source_refs", "authority", "claim"],
  "properties": {
    "schema": { "const": "CHIDDUSH_RECEIPT_V0" },
    "chiddush_id": { "type": "string", "description": "Unique id, e.g. CHID-YYYYMMDD-xxx" },
    "invariant": { 
      "type": "string", 
      "description": "The distilled, portable pattern or principle. Not the story, the invariant." 
    },
    "source_refs": { 
      "type": "array", 
      "items": { "type": "string" },
      "description": "References to the lateral ideas / garden outputs this was compressed from." 
    },
    "category_hints": { 
      "type": "array", 
      "items": { "type": "string" } 
    },
    "confidence": { "enum": ["low", "medium", "high"] },
    "authority": { "const": false },
    "claim": { "const": "NO_CLAIM" },
    "notes": { "type": "string" }
  },
  "additionalProperties": false
}